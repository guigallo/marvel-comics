import React, { Component } from 'react';

export default class ComicCards {
  constructor(props) {
    super(props);
    this.state = { comics: this.props.comics };
  }

  render() {
    return (
      <a></a>
    );
  }
}